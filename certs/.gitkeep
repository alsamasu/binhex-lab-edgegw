# Placeholder for TLS certificates
# Self-signed certificates can be placed here for offline/dev use
#
# Generate self-signed cert:
#   openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
#     -keyout server.key -out server.crt \
#     -subj "/CN=*.local"
